FROM node:20-alpine
WORKDIR /app

COPY package.json ./
RUN npm i

COPY . .

ARG NEXT_PUBLIC_CORE_URL
ARG NEXT_PUBLIC_CORE_HTTP_URL
ENV NEXT_PUBLIC_CORE_URL=${NEXT_PUBLIC_CORE_URL}
ENV NEXT_PUBLIC_CORE_HTTP_URL=${NEXT_PUBLIC_CORE_HTTP_URL}

RUN npm run build

EXPOSE 3000
CMD ["npm", "run", "start"]
